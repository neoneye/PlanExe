# Edit the markdown files in the graph, and regenerate downstream tasks.

Use Flask.

Left side show a DAG editor, in html.

Right side show a markdown editor, with preview.

## Development

Launch the placeholder split-panel app while building the editor:

```bash
flask --app planexe.edit.app run --debug
```
