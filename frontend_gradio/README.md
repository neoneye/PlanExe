# PlanExe (frontend_gradio service)

This directory packages the PlanExe Gradio frontend for Docker and `pip install ./frontend_gradio[gradio-ui]`.

The main project README lives one level up at `../README.md`.
